name: 'shop_analytics_level3'
version: '1.0.0'
config-version: 2

profile: 'shop_analytics_level3'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  shop_analytics_level3:
    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts